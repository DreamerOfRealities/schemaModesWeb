{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        // Run whenever .map.html files are saved
        "match": "(\\.map.html|content.yaml)$",
        "cmd": "echo '${fileBasename}' saved. && python ./tools/transpileHtml.py",
      },
    ],
  },
}